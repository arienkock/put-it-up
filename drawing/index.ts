import { BoardDefinition, BoardRenderer, DrawingContext } from "./types";

export default class DefaultBoardRenderer implements BoardRenderer {
    init(dependencies: DrawingContext) {
        
    }
    
    update(boardDefinition: BoardDefinition) {

    }
}


